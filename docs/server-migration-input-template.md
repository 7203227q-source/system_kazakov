# Данные для переноса сайта на новый сервер

Заполни этот файл и передай мне его целиком. По нему я смогу собрать все исходные данные для миграции сайта, базы данных и сопутствующих сервисов.

## 1. Общая информация

- Название проекта:
- Основной домен:
- Дополнительные домены/поддомены:
- Кратко что переносим:
- Допустимый простой при переносе:
- Желаемое окно миграции:
- Критичность проекта: `низкая / средняя / высокая`

## 2. Исходный сервер

- IP/домен:
- SSH-порт:
- Пользователь:
- Способ входа: `ssh key / пароль`
- Приватный ключ будет предоставлен: `да / нет`
- Нужен `sudo`: `да / нет`
- Root-доступ: `да / нет`
- ОС и версия:
- Архитектура сервера: `x86_64 / arm64 / другое`
- Путь к проекту:
- Где лежат пользовательские файлы:
- Где лежат логи:

## 3. Новый сервер

- IP/домен:
- SSH-порт:
- Пользователь:
- Способ входа: `ssh key / пароль`
- Приватный ключ будет предоставлен: `да / нет`
- Нужен `sudo`: `да / нет`
- Root-доступ: `да / нет`
- ОС и версия:
- Сервер уже подготовлен: `да / нет`
- Нужно установить окружение с нуля: `да / нет`

## 4. Приложение и стек

- Репозиторий GitHub:
- Ветка для продакшна:
- Стек приложения: `Docker / docker compose / Node.js / Python / Django / PHP / Laravel / другое`
- Версия языка/runtime:
- Пакетный менеджер: `npm / pnpm / yarn / pip / poetry / composer / другое`
- Команда сборки:
- Команда запуска:
- Команда перезапуска:
- Используется `systemd`: `да / нет`
- Используется `pm2`: `да / нет`
- Используется `docker compose`: `да / нет`
- Имена сервисов/контейнеров:
- Есть `nginx`/`apache`: `да / нет`
- Пути к конфигам веб-сервера:

## 5. Переменные окружения и секреты

- Где сейчас хранятся переменные окружения: `.env / systemd / docker compose / panel / другое`
- Путь к `.env`, если есть:
- Список обязательных переменных окружения:
- Какие секреты нужно перенести отдельно:
- Можно ли читать текущие секреты на старом сервере: `да / нет`
- Нужна ли замена каких-либо ключей/токенов после миграции: `да / нет`

## 6. База данных

- Тип БД: `PostgreSQL / MySQL / MariaDB / SQLite / другое`
- Версия БД:
- Где работает БД: `на том же сервере / на отдельном сервере / managed service`
- Хост БД:
- Порт БД:
- Имя БД:
- Пользователь БД:
- Пароль будет предоставлен: `да / нет`
- Размер БД примерно:
- Есть ли репликация: `да / нет`
- Нужен перенос всех данных без фильтрации: `да / нет`
- Допустим короткий read-only/maintenance режим на время финального дампа: `да / нет`
- Есть ли большие таблицы/особо критичные данные:
- Есть ли расширения/спецнастройки БД:
- Нужен ли перенос пользователей/ролей БД: `да / нет`

## 7. Файлы и хранилища

- Есть ли директории с загружаемыми файлами: `да / нет`
- Пути к media/uploads:
- Примерный объём файлов:
- Есть ли приватные файлы:
- Используется S3/Cloudflare R2/объектное хранилище: `да / нет`
- Если да, что именно используется:
- Нужно ли переносить локальные бэкапы:
- Нужно ли переносить cron-архивы/экспорты:

## 8. Домены, DNS, SSL

- Где управляется DNS:
- Есть ли доступ к DNS: `да / нет`
- Кто переключает A/AAAA/CNAME:
- Используется Cloudflare: `да / нет`
- SSL сейчас через: `Let's Encrypt / Cloudflare / кастомный сертификат / другое`
- Нужно ли перенести существующие сертификаты: `да / нет`
- Можно ли перевыпустить SSL на новом сервере: `да / нет`
- Есть ли редиректы между доменами:
- Есть ли webhook/callback URL, завязанные на домен:

## 9. Фоновые процессы и автоматизация

- Есть ли `cron`: `да / нет`
- Список cron-задач:
- Есть ли очереди/воркеры: `celery / rq / sidekiq / bullmq / другое`
- Что должно быть запущено постоянно:
- Есть ли планировщики внутри `systemd`/docker:
- Есть ли отдельные службы, кроме сайта и БД:

## 10. Внешние интеграции

- SMTP/почта:
- Telegram-боты:
- Платёжные системы:
- OAuth/Social login:
- Sentry/логирование/мониторинг:
- CDN/WAF:
- API-ключи сторонних сервисов:
- IP whitelist где-то используется: `да / нет`
- Нужно ли менять IP в сторонних кабинетах после миграции: `да / нет`

## 11. Что ещё находится на старом сервере

- Только этот проект или есть другие сайты/сервисы:
- Можно ли трогать системные пакеты:
- Есть ли общий `nginx`/общая БД с другими проектами:
- Есть ли риск затронуть соседние сервисы:

## 12. Ограничения и пожелания

- Нужен перенос без простоя: `да / нет`
- Если без простоя нельзя, максимальный допустимый простой:
- Нужен ли rollback-план: `да / нет`
- Нужно ли сохранить старый сервер как резерв: `да / нет`
- Нужно ли после переноса отключить старый сервер: `да / нет`
- Есть ли требования по безопасности/доступам:
- Есть ли требования по времени выполнения работ:

## 13. Доступы, которые нужно предоставить

Нужно предоставить одно из:

### Вариант A. Доступ прямо к серверам

- SSH к старому серверу
- SSH к новому серверу
- Доступ к БД или возможность делать dump/restore
- Доступ к DNS или контакт того, кто переключит домен

### Вариант B. Через промежуточный jump host / bastion

- SSH к bastion-серверу
- С bastion должен быть доступ к старому серверу
- С bastion должен быть доступ к новому серверу
- Описание цепочки подключения

### Вариант C. Только файлы и доступы без прямого SSH ко всему

- Архив проекта
- Dump базы данных
- `.env`/секреты
- Конфиги `nginx`/`systemd`/`docker compose`
- Доступ к новому серверу

## 14. Команды и пути, если уже известны

```bash
# SSH на старый сервер
ssh -p <PORT> <USER>@<OLD_HOST>

# SSH на новый сервер
ssh -p <PORT> <USER>@<NEW_HOST>

# Путь к проекту
cd <PROJECT_PATH>

# Перезапуск приложения
<RESTART_COMMAND>

# Проверка статуса
<STATUS_COMMAND>
```

## 15. Что прислать вместе с этим файлом

- `README` по деплою, если есть
- текущие `.env` или список переменных
- конфиги `nginx` / `apache`
- `docker-compose.yml`, `compose.yml`, `Dockerfile`, если используются
- unit-файлы `systemd`, если используются
- список cron-задач
- инструкции по запуску/сборке, если они неочевидны
- доступ к DNS/Cloudflare, если переключение домена тоже на мне

## 16. Если не знаешь точные значения

Оставь поле пустым и напиши `нужно определить на сервере`. Тогда я сначала проведу аудит старого сервера и сам дособеру недостающие данные.
